# /etc/nginx/conf.d/fiap-challange-limites.conf
# Freio por IP nas rotas de conta: segura forca bruta antes de chegar no C++.
limit_req_zone $binary_remote_addr zone=challenge_conta:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=challenge_api:10m rate=10r/s;
